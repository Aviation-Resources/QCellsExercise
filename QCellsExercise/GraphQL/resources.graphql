query Documents($limit: Int, $offset: Int) {
  resourceQuery: resources(limit: $limit, offset: $offset) {
    ...Resource
  }
}

fragment Resource on resources {
  id
  updated_at
  created_at
  documentName
  documentNumber
  s3_key
  thumbnail_s3_key
}
